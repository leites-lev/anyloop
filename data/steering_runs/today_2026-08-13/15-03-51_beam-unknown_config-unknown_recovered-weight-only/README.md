# Recovered weight file at 15:03:51

This `observer_weights.dat` appeared at the repository root after an
unrecorded process finalized. No matching config, error stream, command stream,
or console log was captured, so its beam/config provenance is unknown and it
must not be loaded as a validated observer state or used for comparison.
