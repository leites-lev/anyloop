{
	"_comment": {
		"1": "Open-loop plant Bode of the y/tip axis: command -> FSM -> camera -> CoM.",
		"2": "SOFTWARE injection: bode_plot replaces the y element (0; CoM is [y, x])",
		"3": "with a sine each iteration, and piplate_bridge writes offset + sine*scale",
		"4": "to DAC ch0 every cycle. See conf_bode_x.json for the full rationale",
		"5": "(whole-frame CoM, no acquisition phase, baud 921600 pacing, 2310 Hz loop).",
		"6": "x/tilt (ch1) and the coarse channels are parked at their bias voltages.",
		"7": "Amplitude starts lower than x: the y mechanical gain was ~3x x on the",
		"8": "old (stale) sweeps; raise it if |H| comes back small."
	},
	"pipeline": [
		{
			"uri": "file:/home/fsl/anyloop_levfork/anyloop/build/asi_source.so",
			"params": {
				"camera_name": "ASI290MM",
				"width": 64,
				"height": 64,
				"exposure": 330,
				"gain": 100,
				"bandwidth": 100,
				"high_speed": 1,
				"start_x": 920,
				"start_y": 488
			}
		},
		{
			"uri": "anyloop:center_of_mass",
			"params": {
				"_note": "tracked 15x15 window, threshold 30 -- matches attenuation_test.json exactly so the sweep measures the same sensor the loop sees. Initialized on the beam (frame centre) with no acquisition phase: bode_plot injects from the first frame, and the stray reflection at ROI (10,28) must stay out of the sum. Output is image-normalized.",
				"region_height": 15,
				"region_width": 15,
				"threshold": 30,
				"track": true,
				"init_y": 32,
				"init_x": 32
			}
		},
		{
			"_comment": "Replace the y/tip element (0) with the swept sine; read CoM y as response",
			"uri": "anyloop:bode_plot",
			"params": {
				"sample_rate": 2310.0,
				"element": 0,
				"inject": "replace",
				"output": "raw",
				"freq_start": 5.0,
				"freq_end": 300.0,
				"_tuning": "n_freqs 100 / n_settle 400 / n_cycles 40 (2026-07-14): see conf_bode_x.json -- the 240-pt/12-cycle sweep was jagged (worse on y: |P|y*A is only ~0.028, so the ambient lines dominate short correlations, and the 5 Hz point was swamped by the 5.64 Hz line).",
				"n_freqs": 100,
				"n_settle": 400,
				"n_cycles": 40.0,
				"amplitude": 0.15,
				"output_file": "fsm2310_bode_y_bias2.47,1.62_2026-07-14_fine.pdf"
			}
		},
		{
			"_note": "same mapping as steering_tuned.json: ch0=y(idx0, driven), ch1=x(idx1, parked), ch2/ch3 coarse parked. Coarse offsets 2.52/1.62 = current operating point (2026-07-14; was 2.7/1.65 on 07-13, 0.8/2.394 for the 2026-07-10 sweep).",
			"uri": "anyloop:piplate_bridge",
			"params": {
				"port": "/dev/ttyACM0",
				"board": 0,
				"channel": [
					0,
					1,
					2,
					3
				],
				"index": [
					0,
					1,
					0,
					0
				],
				"scale": [
					2,
					0,
					0,
					0
				],
				"offset": [
2,  2, 2.47, 1.62
				],
				"baud": 460800,
				"wait_response": false,
				"skip_unchanged": true
			}
		}
	]
}
