{
	"_comment": {
		"1": "Full-sensor probe: grab 5 frames with the DACs untouched (run it",
		"2": "with the FSM parked at bias) and locate the beam, to re-derive",
		"3": "start_x/start_y for the 56x32 configs after any bias or alignment",
		"4": "change. The coarse channels need ~30 s to stabilize after startup,",
		"5": "so probe only once the beam has settled at its parked position.",
		"6": "Read the beam position from probe_full.aylp:",
		"7": "  (parse: 40-byte header per frame -- magic u32, 4x u8, log_dim y/x u64 at offset 8 -- then y*x uint8s)",
		"8": "start_x = round(beam_x) - 28, start_y = round(beam_y) - 16.",
		"9": "",
		"10": "camera_name/width/height are the ASI662MM's full sensor (1920x1080),",
		"11": "updated 2026-08-02 from the ASI290MM's 1936x1096. When run through",
		"12": "contrib/find_roi.py these three are OVERRIDDEN: find_roi takes the",
		"13": "camera from steering_par_fsp.json and reads the full-sensor size off",
		"14": "the camera itself, so they only matter when this config is run by hand."
	},
	"pipeline": [
		{
			"uri": "file:/home/fsl/anyloop_levfork/anyloop/build/asi_source.so",
			"params": {
				"camera_name": "ASI662MM",
				"width": 1920,
				"height": 1080,
				"exposure": 200,
				"gain": 100,
				"bandwidth": 100,
				"high_speed": 1
			}
		},
		{
			"uri": "anyloop:file_sink",
			"params": { "filename": "probe_full.aylp" }
		},
		{
			"uri": "anyloop:stop_after_count",
			"params": { "count": 5 }
		}
	]
}
